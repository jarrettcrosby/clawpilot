# REQUIREMENTS_TRACEABILITY.md

| Requirement | Source of Truth (Code) | Verification |
|---|---|---|
| PR-001 Documentation coherence gate | `scripts/verify-doc-coherence.sh`, `scripts/dev-verify.sh` | `bash scripts/dev-verify.sh` must pass |
| PR-002 Product routing map | `app_src/lib/agents/routing.ts` (`PRODUCT_TO_EXECUTION`, normalization helpers) | route responses include canonical responder + successful execution |
| PR-003 Assignment normalization | `app_src/app/api/agents/assignments/route.ts`, `app_src/app/api/tasks/[taskId]/claim/route.ts` | assignment PUT + claim POST persist normalized IDs |
| PR-004 Execution + writeback | `app_src/app/api/agents/threads/route.ts`, `app_src/lib/executionWriteback.ts` | task receives status+summary+next-action writeback + comment/activity |
| PR-005 Timeout/hang safety | `app_src/app/api/agents/threads/route.ts` (`runOpenClawAgent`) | real agent thread request returns or fails deterministically |
| PR-006 Nightly reconciliation expectation | `PRODUCT_REQUIREMENTS.md`, `USER_GUIDE.md`, `docs/architecture/AGENT_ROUTING_MODEL.md` | nightly process reads docs+code and reconciles |
| PR-007 UI agent surface coherence | `app_src/lib/types.ts`, `app_src/components/projects/KanbanBoard.tsx`, `app_src/components/projects/CardDetailDrawer.tsx`, `app_src/components/agents/AgentsSection.tsx` | only product agents appear in UI selectors/mentions/dropdowns |
| PR-008 Agent experience quality | `app_src/app/api/agents/threads/route.ts`, `app_src/lib/executionWriteback.ts`, `app_src/components/agents/AgentsSection.tsx`, `app_src/components/projects/KanbanBoard.tsx`, `app_src/components/projects/CardDetailDrawer.tsx`, `scripts/agent-doer-mode-verify.sh` | replies are contextual/actionable with zero-fluff enforcement; execution replies enforce required structure (`Changed`/`Remaining`/`Waiting on`); banned abstract phrases are rejected/rewritten (`summarized context`, `extracted assumptions`, `made progress`, `prepared next step`, `looked into`, `reviewed`, `investigated`); blocker is strict and non-generic; repeated missing-input asks escalate instead of repeating; chat binding is explicit (no hidden auto-selection); assignment/product-agent identity is always visible; send/load failures are surfaced (no silent failure); blocked move-to-active opens drawer + inline actionable hints (owner highlight / next-action focus) to enable immediate repair |
| PR-009 Operator priority panel | `app_src/components/dashboard/DashboardSection.tsx`, `app_src/components/agents/AgentsSection.tsx` | dashboard shows top 3–5 ranked tasks with why-now, blocker, next-action, owner, and direct actions (open task/open chat/assign) |
| PR-010 Task reality state | `app_src/lib/taskRealityState.ts`, `app_src/components/dashboard/DashboardSection.tsx`, `app_src/components/projects/CardDetailDrawer.tsx` | derived ACTION_REQUIRED/IN_PROGRESS/BLOCKED/DONE_CONFIRMED/DONE_UNCONFIRMED states drive prioritization, completion reconciliation summary, and completion-awareness labels |
| PR-011 Task creation policy | `app_src/app/api/tasks/route.ts`, `app_src/app/api/task-creation-audit/summary/route.ts`, `app_src/components/dashboard/DashboardSection.tsx`, `app_src/components/agents/AgentsSection.tsx`, `docs/governance/TASK_CREATION_POLICY.md`, `docs/operations/TASK_CREATION_CONTAINMENT_2026-03-17.md` | create requires explicit `_createSource` + `_actor`; agent-originated create blocked; successful creates are auditable; >3 creates/<1m flagged; dashboard shows 24h count + last source/actor; suggestion-to-task remains default-deny |
| PR-012 Canonical work-item enforcement | `app_src/lib/workItemModel.ts`, `app_src/app/api/tasks/route.ts`, `app_src/app/api/agents/assignments/route.ts`, `app_src/app/api/agents/threads/route.ts`, `app_src/app/api/pipeline/route.ts`, `scripts/verify-work-model-invariants.sh`, `scripts/dev-verify.sh` | canonical work-item comes from tasks; agents/pipeline read projected canonical state; invariant gate blocks on status/assignment/next-action divergence |
| PR-013 Hard task quality + board hygiene enforcement | `app_src/app/api/tasks/route.ts`, `scripts/verify-board-hygiene.sh`, `scripts/dev-verify.sh`, `scripts/promotion-dry-run.sh`, `scripts/promotion-execute.sh`, `docs/operations/promotion.md` | invalid task creation is hard-blocked (`TASK_INVALID_QUALITY`); only hard-block junk is hidden from active backlog/todo; governance-labeled salvageable cards stay visible; promotion blocks with `PROMOTION_BLOCKED_BOARD_HYGIENE` when hard-block junk exists; dev-verify fails on active hard-block hygiene violations |

## Reality Check Template (required per slice)
1. What changed in system behavior?
2. Which requirement(s) does it map to?
3. Do docs already reflect this?
4. If not, what docs were updated?
5. Evidence (commands, API responses, file paths)
| PR-014 Stale task triage panel | `app_src/components/projects/KanbanBoard.tsx`, `app_src/components/projects/NeedsAttentionPanel.tsx`, `app_src/lib/staleTasks.ts` | stale in-progress tasks appear in Needs Attention panel (top 5) and one-click triage actions update task state/comment/activity immediately |
| PR-015 Canonical state truth | `app_src/lib/workItemModel.ts` (`deriveStateTruth`), `app_src/components/projects/KanbanCard.tsx`, `app_src/components/projects/CardDetailDrawer.tsx`, `app_src/components/agents/AgentsSection.tsx`, `app_src/components/dashboard/DashboardSection.tsx` | same task shows same `[State Chip] — Reason` across Projects/Agents/Dashboard with deterministic precedence |
| PR-016 Next action guidance | `app_src/lib/workItemModel.ts` (`deriveNextActionGuidance`), `app_src/components/projects/KanbanCard.tsx`, `app_src/components/projects/CardDetailDrawer.tsx`, `app_src/components/dashboard/DashboardSection.tsx`, `app_src/components/agents/AgentsSection.tsx` | same task shows same deterministic `👉 Next: ...` guidance aligned with State Truth across Dashboard/Projects/Agents surfaces |
| PR-017 Closure authority + single triage path | `app_src/lib/workItemModel.ts` (`deriveClosureAuthority`, `deriveStateTruth`, `deriveNextActionGuidance`), `app_src/components/projects/CardDetailDrawer.tsx`, `app_src/components/dashboard/DashboardSection.tsx` | closure is binary (closable vs not closable), closable always shows `Review and close task`, non-closable shows exactly one triage action, and conflicting `Likely done` decision labels are suppressed |
| PR-018 Now Working neutral no-recent-run state | `app_src/lib/nowWorking.ts`, `app_src/components/dashboard/DashboardSection.tsx`, `app_src/components/projects/KanbanBoard.tsx` | for `No recent run`, UI shows neutral empty-state text without a highlighted task and hides `Continue in Agents`; `Now working`/`Waiting on` behavior unchanged |
| PR-019 Resolve stale in-progress batch action | `app_src/components/projects/KanbanBoard.tsx`, `app_src/lib/nowWorking.ts`, `app_src/app/api/agents/threads/route.ts`, `app_src/app/api/tasks/route.ts` | stale candidates are `in-progress` + no recent thread + no recent evidence (>72h); Projects shows `👉 Resolve stale work`; apply requires explicit mode (`todo` or `waiting` with reason) and updates canonical task state via PATCH; surfaces refresh from updated task truth |
| PR-020 Shared live-state derivation consistency | `app_src/lib/liveState.ts`, `app_src/components/projects/KanbanCard.tsx`, `app_src/components/agents/AgentsSection.tsx`, `app_src/components/dashboard/DashboardSection.tsx` | same task snapshot renders the same live-state label/reason/next guidance across Projects, Agents, and Dashboard (no divergent surface-specific fallback logic) |
| PR-021 Projects mobile card usability (hotfix) | `app_src/components/projects/KanbanBoard.tsx` | at <=600px Projects hides auxiliary panels, enables mobile vertical recovery scroll, and keeps cards visible/readable/tappable in initial viewport |
| PR-022 Trello-style card interaction + agent hand-off | `app_src/components/projects/KanbanCard.tsx`, `app_src/components/agents/AgentsSection.tsx` | touch tap opens drawer in one action; assigned cards provide direct `Open chat` hand-off into task-linked agent thread |
| PR-023 Agents context auto-bind | `app_src/components/agents/AgentsSection.tsx` | selecting an agent with no selected task auto-binds chat context to the most recently updated assigned task |
| PR-024 Projects card quick advance + scanability | `app_src/components/projects/KanbanCard.tsx` | card metadata is condensed for scanability; `Next` action advances task to next canonical status using existing PATCH flow |
| PR-025 Agents default focus mode | `app_src/components/agents/AgentsSection.tsx` | non-essential tools are collapsed behind `Show tools`; task-linked collaboration controls remain primary in default view |
| PR-026 Projects stale focus filter | `app_src/components/projects/KanbanBoard.tsx` | stale banner provides one-click `Focus stale only` toggle; board can be temporarily narrowed to stale in-progress candidates and auto-resets when no stale candidates remain |
