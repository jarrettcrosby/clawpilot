{
  "name": "clawpilot-shopify-extensions",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "build": "shopify app build",
    "config:link": "shopify app config link",
    "deploy:staged": "shopify app deploy --no-release",
    "shopify": "shopify",
    "test": "npm --workspaces --if-present test"
  },
  "devDependencies": {
    "@shopify/cli": "4.5.2"
  }
}
