# Pointers to update when adopting

- OpenClaw workspace MEMORY.md: add link to deployment contract
- AGENTS.md: add contract pointer
- App repo docs/operations/development-contract.md: add contract pointer
