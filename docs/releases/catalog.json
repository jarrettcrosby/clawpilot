{
  "schemaVersion": 1,
  "releases": {
    "default": {
      "oncePerEnvironment": true,
      "title": "Organization accounting, exact CRM history, and monitored operations",
      "summary": "Each active business can now bind its own QuickBooks Online company, refresh a read-only accounting catalog, import selected products, and map Toast locations without enabling financial writes. CRM history preserves distinct calendar and interaction identity, while scheduled environment checks and verified recovery controls make database and worker failures visible.",
      "features": [
        "A New opportunity workflow available to pipeline owners and editors",
        "Tenant-scoped selection of existing CRM customer organizations",
        "Inline customer organization creation without leaving the opportunity workflow",
        "Configured product selection for new and existing opportunities",
        "Immediate CRM-backed opportunity visibility while SuiteCRM and workbook synchronization continues",
        "Expected close date and notes fields during opportunity creation",
        "Visible pending and failed CRM synchronization counts",
        "Bounded autonomous agent continuations that produce a persisted research or design deliverable and complete only evidenced checklist work",
        "Exact many-to-many Contact relationships for Opportunities in ClawPilot and SuiteCRM",
        "Organization-scoped Contact, controlled Type, and active ClawPilot Agent selectors for interactions",
        "Administrator mapping from ClawPilot users to native SuiteCRM users",
        "Task-linked agent working documents that consolidate research and design output behind concise card-comment links",
        "Explicit Work and Discuss modes so private conversation cannot silently mutate shared task evidence",
        "Visible queued, working, input-needed, blocked, and review-ready states with direct access to the working document and next run",
        "Browser-session security details that identify the authenticated user, support-mode effective user, and observed sign-in network addresses",
        "Independent EPISCS ownership for the established Sales CRM graph, pipeline workbook, record links, documents, and CRM Board",
        "Organization-bound QuickBooks Online connections with read-only company, chart-of-accounts, and product/service catalog synchronization",
        "Explicit QuickBooks item import into each organization's CRM product catalog and per-location Toast accounting mappings",
        "Credential-free scheduled monitoring of development and production database, authentication, CRM, accounting, and agent worker health"
      ],
      "fixes": [
        "Preserves meaningful card descriptions up to 10,000 characters instead of replacing detailed scope with generic text",
        "Prevents provider or worker transport success from marking requested work complete",
        "Records exact task mutations as execution evidence and reports no deliverable changed when nothing persisted",
        "Keeps autonomous work triaged, awaiting input, or blocked until separate completion evidence exists",
        "Makes assignment retries idempotent so generated checklist items are not duplicated",
        "Requires idempotency keys for opportunity creation to prevent duplicate CRM records after network retries",
        "Makes opportunity creates and edits replay-safe so delayed retries cannot overwrite newer CRM changes",
        "Displays persisted expected-close dates correctly when PostgreSQL returns date columns as JavaScript Date values",
        "Persists product changes made from the opportunity drawer",
        "Preserves blocked, awaiting-input, and triaged agent outcomes across worker retries and ignores stale dispatch results",
        "Removes direct guessed Google Sheet row writes from opportunity creation and editing",
        "Propagates Organization renames to pipeline opportunities and generated workbook projections through the canonical Organization relationship",
        "Writes interaction Account and Contact relationships to SuiteCRM History while assigning the mapped native CRM user",
        "Preserves past interaction dates through SuiteCRM synchronization by separating the Occurred At business timestamp from the Note creation timestamp",
        "Opens Activity targets in the event's owning board or pipeline instead of the currently selected workspace resource",
        "Keeps local development usable when hosted browser-session authentication is disabled",
        "Prevents continuation runs from repeating checklist work by supplying the exact next item ID and accepting an exact legacy item label",
        "Rejects malformed forwarded addresses and keeps raw browser-session IP data out of the general activity stream",
        "Rejects overlapping manual agent runs across browser sessions with a task-scoped transactional lock",
        "Prevents discussion replies and unsourced current-research claims from being recorded as completed task work",
        "Sanitizes agent-provider failures so credentials and upstream authorization details never appear in task threads",
        "Restores authenticated API behavior for the explicitly unauthenticated, non-hosted local development runtime",
        "Keeps Suburbia memberships and child-workspace CRM projections isolated while moving all Sales customer contacts, opportunities, interactions, meetings, products, and history to EPISCS",
        "Makes EPISCS the final SuiteCRM primary Account when a migrated Contact also has a generated Suburbia projection",
        "Prevents a configured owner with multiple root businesses from binding the same Google Sheet to two pipelines",
        "Separates same-subject calendar and interaction rows by stable identity so distinct CRM history is not presented as an accidental duplicate",
        "Retires approved production test interactions and meetings while preserving auditable delete and synchronization evidence",
        "Invalidates stale QuickBooks mappings when a connection changes or catalog entries become inactive"
      ]
    }
  }
}
